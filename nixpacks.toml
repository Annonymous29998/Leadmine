[variables]
NIXPACKS_NODE_VERSION = "20"
# Keep Vite / TypeScript available during install + build
NPM_CONFIG_PRODUCTION = "false"
NPM_CONFIG_INCLUDE = "dev"

[phases.install]
cmds = ["npm ci --include=dev"]
